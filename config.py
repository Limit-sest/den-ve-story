skip_ig = False# Only generate the img (default: False)
skip_sleep = False# Generate (and upload) image instantly without waiting for new day (default: False)
use_show = False# Call img.show() from PIL after generating the img (default: False)
random_delay = True# Add random delay before posting so instagram is less likely to ban you (default: True)
random_delay_range = (1, 1800)# Range for the random wait time in seconds (default: (1, 1800))
use_session_id = True